# FPVSIM — специфікація гри

Симулятор FPV-дрона від першої особи на Three.js. Кампанія з рівнів: зліт з трави,
пошук цілі по квадратах, вихід на ціль. Без стрілок-маркерів, без міні-мапи в польоті.
Ядро фану — **керування** (кожен дрон і кожне навантаження літає інакше) і **пошук**
(ти реально не знаєш, де ціль, поки не побачиш її на брудному аналоговому відео).

---

## 1. Стек і межі

| Що | Рішення |
|---|---|
| Рендер | Three.js (WebGL2), r170+ |
| Мова | TypeScript, Vite |
| Фізика | **власний rigid-body для дрона** (не Rapier/Cannon), Rapier тільки для колізій/ландшафту |
| Стан | простий ECS-lite: `World` + системи, без Redux/сторонніх |
| Асети | процедурні + низькополі GLTF; ціль — <25 МБ на рівень |
| Таргет | 60 FPS на M1 / GTX 1650, 1080p |
| Платформа | десктоп-браузер, геймпад + клавіатура (мобайл — ні) |

**Anti-goals:** мультиплеєр, редактор рівнів, реалістична деструкція, фотореалізм,
жодного «арт-стилю мілітарі-шутера». Естетика — сухий документальний аналог.

---

## 2. Модель польоту (серце гри)

Дрон — тверде тіло з 4 моторами. Симуляція йде на **фіксованому кроці 500 Гц**
(субкроки всередині кадру), рендер — вільний.

### 2.1 Стан
```
position, velocity        // світові
quaternion, angularVel    // тіло
motorRPM[4]               // з інерцією ротора
battery: {mAh_used, sagV}
payload: {mass, comOffset, dragArea}
```

### 2.2 Сили
1. **Тяга:** кожен мотор `T_i = kT * rpm_i²`, напрям — вісь Z тіла. Момент `Q_i = kQ * rpm_i²` (yaw).
2. **Інерція ротора:** RPM іде до цілі через лаг `tau_motor` (30–90 мс залежно від дрона). Це і є «в'ялість».
3. **Опір:** квадратичний, анізотропний (`Cd_xy`, `Cd_z`), + додаткова площа від навантаження.
4. **Просадка батареї:** при газі >70% напруга падає → тяга падає. Наприкінці польоту дрон «важчає».
5. **Вітер:** постійний вектор + пориви (Perlin у часі), + турбулентність біля землі та за будівлями.
6. **Ground effect:** +8–12% тяги нижче 1.5× діаметра пропа. Відчувається на зльоті з трави.

### 2.3 Керування
- Режим один — **ACRO, на всіх рівнях без винятку**. Автовирівнювання немає ніде:
  що виставив стіком, те й лишається, поки не рухаєш його знову. Зовнішній контур
  по куту (`flight/angle.ts`) лишився тільки для автопілота в тестах.
- Вхід: roll/pitch/yaw = цільова кутова швидкість (°/с), throttle = сирий газ.
- **Rates:** RC-крива Betaflight-style (`rcRate`, `super`, `expo`) — per-drone, і гравець може крутити їх у налаштуваннях дрона.
- **PID-луп** на кутовій швидкості. Гравець PID не бачить, але вони різні по дронах:
  недотюнений дрон дає **пропвош** (тряска на швидкому спуску) і **осциляції** після різкого стіка.
- Мертвої зони немає. Auto-level немає. Дрон не зависає сам — газ тримає гравець.

### 2.4 Що робить навантаження (payload)
Payload — це три числа, і вони змінюють все:
- `mass` → менше запасу тяги, довший розгін, тонеш при різкому розвороті.
- `comOffset` (зміщення центру мас вниз/вперед) → маятник: після різкого стіка дрон гойдається, pitch «залипає».
- `dragArea` → менша максимальна швидкість, сильніший знос вітром, довше гальмування.

Плюс `armDistance` — до цього віддалення від точки зльоту фінальний удар не зараховується
(геймплейний запобіжник, а не реалізм). Індикатор — одна літера в OSD.

---

## 3. Дрони (класи) і як вони мають відчуватись

| # | Клас | Вага/тяга | Payload | Відчуття |
|---|---|---|---|---|
| 1 | 7" тренувальний | T/W 4.0 | немає | легкий, прощає, майже без інерції |
| 2 | 7" легкий | T/W 2.6 | легкий | помітна інерція, ще швидкий |
| 3 | 8" середній | T/W 2.0 | середній | треба планувати гальмування за 2 с наперед |
| 4 | 10" важкий | T/W 1.5 | важкий | летить як баржа, розворот радіусом 30 м, просадка на газі |
| 5 | 10" з об'ємним навантаженням | T/W 1.6 | великий `dragArea` | вітер зносить, максималка 45 км/год |
| 6 | 13" далекобійний | T/W 1.4 | середній | 8 хв польоту, але керування «через кисіль» (лаг мотора 90 мс) |
| 7 | нічний 8" | T/W 2.0 | середній | камера — ЧБ, шум ×3, видимість 120 м |

Подвоєння тяги пробували 07.08.2026 і відкотили того ж дня: швидше — так,
але відчуття класів злипалось (важкий БК майже не сповільнював обертання).
Якщо «довго летіти» повернеться — крутити треба маршрути/зони, а не фізику.

Кожен дрон має власні: `tau_motor`, PID-якість, ліміт кутової швидкості, ємність батареї,
потужність відеопередавача (радіус до перших перешкод у сигналі).

---

## 4. Структура рівня

### 4.1 Брифінг (перед вильотом)
Топографічна мапа 2D (canvas, не 3D):
- сітка квадратів з підписами (`A1…H8`), масштаб, північ;
- **зона ймовірної цілі** — заштрихований еліпс на 2–4 квадрати, не точка;
- текст завдання: «розвідка автоколони на ґрунтівці між C4 і E6, час — до 6 хв»;
- точка зльоту, напрям і сила вітру, залишок заряду.

Мапу можна вивчати скільки завгодно. **Після зльоту мапа недоступна.** Гравець запам'ятовує.

### 4.2 Політ
1. **Зліт з трави.** Камера в траві, ground effect, перші 3 м — найтрусніші.
2. **Маршрут.** Компас-стрічка + висота + таймер — усе, що є з навігації.
3. **Пошук.** Гравець сам обирає висоту огляду: вище — видно квадрат цілком, але дрібно
   й видно тебе; нижче — деталі, але за деревами нічого.
4. **Ідентифікація.** Цілі схожі на цивільні об'єкти. Помилка = провал рівня (див. §6).
5. **Захід.** Фінальна ділянка — 200 м, де рулять інерція, вітер і затримка відео.

### 4.3 Умови завершення
- **Успіх:** ураження призначеної цілі.
- **Провал:** батарея 0 / втрата сигналу >8 с / зіткнення з рельєфом / удар по цивільному об'єкті / вихід таймера.

---

## 5. Цілі

| Тип | Поведінка |
|---|---|
| Стаціонарна техніка | стоїть під маскувальною сіткою; помітна тільки з правильного кута |
| Вантажівка | їде маршрутом 20–40 км/год, зупинки, може зайти в лісосмугу |
| Колона з 2–3 машин | треба обрати одну (пріоритет у брифінгу) |
| Укриття/позиція | статична, але вхід видно лише з одного боку |
| Приманка (decoy) | макет: видно з 100 м, з 40 м — інші пропорції, немає тіні від корпусу |

**Цивільні об'єкти** їздять тими ж дорогами. Розрізняються силуетом і кольором.
Обов'язковий елемент — гравець мусить *дивитись*, а не «летіти в те, що рухається».

Рух — прості waypoint-маршрути + невеликий шум швидкості. Ніякого AI-стрибання.

---

## 6. Правила ідентифікації

- Ціль вважається ідентифікованою, якщо гравець утримав її в кадрі ≥1.5 с з <80 м.
  Тоді OSD один раз блимає `ID`. Це **не маркер** і не трекер — просто підтвердження, що ти бачив.
- Удар по цивільному об'єкту → миттєвий провал, екран `MISIDENTIFIED`, рівень перезапускається.
- Удар по decoy → провал завдання, але без «штрафу», з підказкою в дебрифі.

---

## 7. Камера, відео і HUD

Це задає весь настрій. Три шари постпроцесу:

1. **Аналоговий сигнал:** горизонтальні смуги шуму, залежні від
   `distance × перешкоди × потужність VTX`. Повна втрата → сірий сніг і чорний екран.
2. **Об'єктив:** FOV 120–150° (per-drone), barrel distortion, вигоряння неба,
   rolling-shutter желе на вібраціях.
3. **OSD** (зелений монотонний шрифт, кути екрана):
   `V 21.8` (напруга) · `ALT 84` · `SPD 62` · `TIME 03:12` · компас-стрічка зверху.
   Все. Ніякої мінімапи, стрілок, підсвітки ворогів, хіт-маркерів.

Затримка відео **90–140 мс** (per-drone) — реальний фактор складності на швидкості.

Звук: тон моторів від RPM (WebAudio, 4 осцилятори + шум), свист повітря від швидкості,
статика радіо синхронна з шумом картинки, вітер. Голосу і музики в польоті немає.

---

## 8. Світ

- Ландшафт: heightmap 2×2 км, чанки, LOD. Ґрунтівки, лісосмуги, поля, кілька будівель.
- Рослинність: instanced mesh, трава — тільки в радіусі 60 м, дерева до 800 м з імпостерами.
- Колізії: дрон vs terrain + дерева/будівлі (Rapier, статичні колайдери).
- Освітлення: 4 пресети часу доби (світанок / день / вечір / ніч+ЧБ-камера) + туман.
- **Продуктивність важливіша за красу.** Бюджет: <1.5 M трикутників у кадрі, <120 draw calls.

---

## 9. Кампанія (15 рівнів)

Розмір цілі і є складністю: мотоцикл (2.1 м, радіус ураження 1.8 м) треба шукати
впритул і влучати майже впритул, танк (9.5 м) видно здалеку, але він під сіткою
або в лісосмузі. Каталог техніки — `src/game/targets.ts`, рівні задають лише клас.

| # | Рівень | Дрон / БК | Погода | Ціль(і) | Що вчить |
|---|---|---|---|---|---|
| 1 | Перший виліт | 7" тренувальний | штиль | укриття (стат.) | ACRO, зліт, вихід на ціль |
| 2 | Ґрунтівка | 7" легкий + легкий | вітер 4.5 | вантажівка | ACRO, розрізнення цивільних |
| 3 | Мотоцикл з БК | 7" легкий + легкий | штиль | мотоцикл (маршрут) | дрібна швидка ціль, упередження |
| 4 | Мотоцикл | 7" легкий + легкий | вітер 4.5 | мотоцикл | найменший силует у грі |
| 5 | Два вильоти | 8" середній + середній | штиль | вантажівка + пікап | **2 вильоти**, різні кути карти |
| 6 | БМП у лісосмузі | 8" середній + середній | вітер 4.5 | БМП під сіткою | маскування, макет поруч |
| 7 | Танк | 10" важкий + важкий | штиль | танк | інерція баржі, планування заходу |
| 8 | Три вильоти | 8" середній + середній | вітер 4.5 | мотоцикл, вантажівка, БМП | **3 вильоти**, три кути карти |
| 9 | Вітер 8 м/с | 7" легкий + об'ємний | пориви | вантажівка | знос вітром, поправка на заході |
| 10 | Сутінки | 8" середній + середній | вечір | БМП серед 2 макетів | ідентифікація на низькому контрасті |
| 11 | Далекий рейд | 13" далекобійний | штиль, карта 4×4 км | танк за 2.5 км | межа сигналу, лаг 140 мс |
| 12 | Ніч | 8" нічний + середній | ніч | вантажівка | ЧБ-камера, видимість 120 м |
| 13 | Пара | 10" важкий + важкий | вітер 4.5 | танк + БМП | **2 вильоти** бронетехніки |
| 14 | Туман | 8" середній + середній | туман 700 м | вантажівка + мотоцикл | **2 вильоти**, навігація по дорогах |
| 15 | Все разом | 10" важкий + об'ємний | ніч + вітер | БМП, танк, вантажівка | **3 вильоти**, найважчі умови |

**Вильоти.** Рівень із кількох вильотів проходиться поспіль: кожен виліт — свіжий борт,
своя точка зльоту, своя ціль. Провал будь-якого повертає рівень до першого вильоту.
Цілі попередніх вильотів прибираються зі світу, щоб не можна було зарахувати одне влучання двічі.

---

## 10. Архітектура коду

```
src/
  core/        loop.ts (fixed-step), world.ts, input.ts (gamepad+kbd), audio.ts
  flight/      rigidbody.ts, motors.ts, pid.ts, rates.ts, aero.ts, battery.ts
  drones/      index.ts + drone-*.json   ← весь тюнінг у даних, не в коді
  level/       loader.ts, level-*.json, terrain.ts, vegetation.ts, targets.ts
  ai/          route.ts (waypoints), convoy.ts
  render/      scene.ts, camera-fpv.ts, post/ (noise, distortion, osd)
  ui/          briefing-map.ts, debrief.ts, settings.ts
  game/        mission.ts (стани, умови провалу), identify.ts
```

Правила:
1. **Уся поведінка дрона — в JSON.** Новий дрон = новий файл, нуль рядків коду.
2. **Політ не знає про рендер.** `flight/` не імпортує Three.js.
3. **Фіксований крок фізики.** Ніяких `deltaTime` у формулах сил.
4. Рівень — теж JSON: рельєф seed, маршрути, зона брифінгу, погода, дрон.
5. Дебаг-оверлей (`~`): графіки RPM, кутової швидкості, тяги, споживання.

---

## 11. Стан і етапи

### Зроблено
- **Ядро польоту** (`src/flight/`) — rigid body 500 Гц, мікшер X-конфігурації в стилі
  Betaflight (airmode), лаг моторів, PID на кутовій швидкості, RC-криві, анізотропний опір,
  ground effect, пропвош, вітер із зсувом і поривами, батарея з P ∝ T^1.5 і просадкою.
- **Payload-система** — маса / зміщення ЦМ / площа. Маятник і флюгерування — наслідок фізики,
  а не скрипт.
- **6 дронів + 5 БК** — цілком у даних (`src/drones/index.ts`).
- **Ігровий шар** (`src/game/`, `src/level/`) — процедурний рельєф із seed, сітка A1–H8,
  рухомі цілі на маршрутах, цивільні, макети, ідентифікація за часом у кадрі, модель сигналу,
  умови провалу.
- **Наповнення карти** (`src/level/props.ts`) — лісосмуги, чагарник, будинки, стоги, ЛЕП.
  Тверді **всі без винятку**: дерево, кущ, будівля, стіг, щогла ЛЕП і окремо траверса
  ЛЕП — зіткнення = провал із назвою об'єкта в дебрифі. Генерація детермінована
  й спільна для рендера та фізики; зони навколо зльотів і маршрутів лишаються чистими.
- **Пояснення керування** при першому запуску (`src/ui/tutorial.ts`), далі за кнопкою.
- **Рендер** (`src/render/`) — процедурний світ (рельєф із полями, лісосмуги, дороги, трава),
  FPV-камера з реальною затримкою відео, аналоговий тракт одним проходом.
- **UI** (`src/ui/`) — брифінг-мапа з відмивкою рельєфу і зоною пошуку, OSD, дебриф.
- **Картки цілей у брифінгу** (`src/ui/target-preview.ts`) — модель кожної цілі рівня
  повільно обертається на місці, з габаритами, радіусом ураження, швидкістю і сіткою.
  Це не маркер: у польоті підказок немає, і силует доводиться запам'ятовувати тут.
  Усі картки малює ОДИН WebGL-контекст і копіює в 2D-канвас — на рівень із трьох
  вильотів інакше йшло б три контексти.
- **15 рівнів, 22 вильоти** — цілком у даних, з каталогом техніки від мотоцикла до танка
  й мультивильотними рівнями.
- **Прогрес кампанії** в localStorage: пройдені рівні, поточний виліт, послідовне
  відкриття рівнів, екран вибору рівня зі скиданням. Пройдені рівні перепроходяться
  вільно; вихід у меню — з брифінгу, дебрифу та з польоту (`Esc`).
- **Окремого «арму» немає:** мотори піднімаються від першого руху газу. `Space`
  лишається аварійним глушінням у польоті.
- **Галерея моделей** (`models/`) — сторінка показує ФАКТИЧНІ моделі гри: імпортує
  `buildVehicle()` і білдери пропсів із `src/render/`, панелі — види з рівнів
  (target/civilian/decoy/під сіткою) і варіанти пропсів. Колишній пікер трьох
  стилів свою роль відіграв і видалений. Не входить у збірку: `npm run dev` → `/models/`.
- **Моделі з вибору перенесені в гру** (`src/render/vehicles.ts`, `src/render/props-models.ts`):
  по одному стилю на клас техніки і по 2–3 варіанти на кожен вид наповнення — варіант
  об'єкта визначається хешем його координат, тому карта виглядає однаково при кожному
  запуску рівня. Геометрія склеюється в один меш на матеріал (`src/render/shapes.ts`),
  інакше детальні моделі з'їдають увесь бюджет draw calls.
- **Будівлі — запечені GLB** (`assets/suburban/`, Kenney City Kit Suburban 2.0, CC0):
  8 типів будинків замість двох процедурних ферм. Кольори — семплінг palette-текстури
  по UV у вершинні кольори при запіканні. Головне правило: **колайдер кроїться під
  модель, а не навпаки** — `props.ts` обирає тип (`Prop.variant`) і виводить
  габарити з пропорцій моделі (фасад 10–20 м, висота ≤ 12.5 м).
- **Дерева — запечені OBJ** (`assets/trees/`, Low Poly Tree Pack): 7 варіантів —
  листяні, берези, сосни. Висота рівно `TREE_H` (закріплено тестом), крона
  в природних пропорціях і навмисно ширша за вузький колайдер стовбура.
  Бейкер парсить OBJ+MTL сам (Kd — лінійний простір, як baseColorFactor).
- **Техніка — запечені GLB** (`assets/mil/`, `scripts/bake-vehicles.mjs`, `npm run bake`):
  мотоцикл, пікап (джип), фургон (санітарка), вантажівка, БМП і танк — низькополі GLB
  з військового набору, конвертовані в статичні дані (`src/render/vehicle-data.ts`):
  один меш із вершинними кольорами на машину, тому buildVehicle лишився синхронним
  (тести, картки брифінгу) і 1 draw call на борт. Ширина — точно з каталогу, довжина
  й висота — спільним множником у ±30% від каталогу, щоб колеса лишались круглими.
  «Свій/чужий» — перефарбуванням оливкових кольорів у вершинах (цивільний беж / сірий
  макет). Легковик і укриття процедурні: у наборі немає седана й насипу;
  Helicopter, Military Boat і Bike (велосипед) класу в грі не мають.
- **Огляд моделей у світі** — `probe.html` (`npm run dev` → `/probe.html`): ставить камеру
  куди скажуть і малює один кадр, без польоту. Теж не входить у збірку.
- **UI повністю англійською.** Коментарі в коді й назви тестів лишаються українськими —
  це документація для розробника, а не текст для гравця.
- **Повтор ураження** (`src/game/replay.ts`, `src/render/replay.ts`) — після успіху:
  0.7 с «сигнал втрачено», потім ~7 с ЧБ-повтору з борта спостерігача. Рекордер пише
  хвіст польоту (~30 Гц, кільцевий буфер), в'юха ставить статичну камеру збоку від
  лінії заходу, веде записаний борт і цілі; техніка лишається в кадрі. Той самий
  аналоговий тракт (`VideoFeed.setMono`). Скіп — будь-яка клавіша. Провали йдуть
  одразу в дебриф.
- **Вибух** (`src/render/explosion.ts`) — три варіанти з чистим `update(t)`
  (детерміновано від сіда, тому /models лупить по колу, а гра грає раз):
  ударна хвиля, балістичні уламки, іскри, дрібні клуби диму, вогняна куля,
  вторинні детонації, обгоріла пляма. У реплеї — обраний юзером «Dust & debris»;
  прев'ю всіх трьох — /models → Effects (сторінка вміє анімувати через
  `group.userData.tick`).
- **310 тестів** — фізика, мікшер, батарея, аеродинаміка, правила місії, каталог техніки,
  чесність рівнів, геометрія моделей, прогрес і автопілот, що проходить **усі 22 вильоти**
  від зльоту до влучання.

### Далі
| Етап | Результат | Оцінка |
|---|---|---|
| M9 | налаштування rates і чутливості в UI | 1 день |
| M10 | звук по класах техніки, дим після ураження | 1 день |
| M11 | руйнування цілі: дим, уламки | 1 день |

---

## 11a. Уроки, оплачені кров'ю

Це не історія — це граблі, на які легко наступити вдруге.

1. **`quatToEuler().yaw` — компасний курс (за годинниковою від півночі), не математичний.**
   Математична конвенція дає протилежний знак і ламає одночасно OSD й будь-яке наведення.
   Закріплено тестами в `tests/math.test.ts`.
2. **`pitch+` = ніс УГОРУ.** Щоб летіти вперед, потрібен від'ємний нахил. Автопілот у тестах
   уже раз літав хвостом уперед.
3. **Хеш шуму рельєфу — тільки `Math.imul`.** Велика константа у звичайному множенні
   виходить за точність float64, і для частини сідів рельєф вироджується в ідеальну площину.
   Перевіряти треба ВСІ сіди рівнів, а не один.
4. **Render target у three.js — лінійний простір.** Перетворення в sRGB three.js при рендері
   в буфер не робить; його виконує наш шейдер (`src/render/video.ts`). Без цього все нижче
   горизонту чорне.
5. **Mission не бере нічого з `drone.telemetry`.** Телеметрію оновлює лише фізичний крок,
   а правила місії мусять бути істинними завжди — тому висота й зведення рахуються в `Mission`.
6. **Натискання клавіш — подією, не опитуванням.** Тап між двома кадрами інакше зникає.
7. **Клавіатура — не стік.** Без асистів будь-який тап стрілки = повна кутова
   швидкість, тому осям потрібен розгін; геймпад лишається абсолютним. Газ —
   **трещітка** (07.08.2026): W/S рухають, відпустив — стоїть де поставив.
   Повернення-до-зависання прибрано на прохання пілота: воно тягнуло газ до 90%+
   на важких бортах і не давало виставити крейсер. Висота в нахилі — робота пілота.
8. **Rates — фристайлові, не гоночні** (110–310 °/с). Гоночні 500–800 °/с роблять
   точний захід на ціль неможливим. Закріплено тестом у `tests/mixer.test.ts`.
9. **Цивільні НЕ можуть стояти на точках маршруту цілі.** Спільна точка = дві машини
   в одному місці, і гравець фізично не може їх розрізнити — це нечесний провал,
   а не помилка пілота. Закріплено тестом у `tests/campaign.test.ts`.
10. **Рендер і колізії беруть об'єкти з ОДНОГО джерела.** Два незалежні генератори
    означали б, що гравець розбивається об дерево, якого не бачить.
11. **Зіткнення не рахуються на «стрибках» позиції** (> 60 м за кадр): відновлення
    вкладки чи телепорт у тесті інакше дають вигадане зіткнення на пів карти.
12. **Модель не має вилазити за свій колайдер.** Дах будівлі колись стирчав над
    колайдером — крізь нього можна було пролетіти наскрізь; траверса ЛЕП розкинута
    на 4.3 м, а колайдер щогли має радіус 0.6 м, тому траверсі потрібна власна плита.
    Правило просте: що видно — об те й б'ються, і навпаки. Закріплено
    в `tests/render.test.ts`, бо скріншот такого не ловить.
13. **Захід на наземну ціль мусить бути пологим (~15°).** Пікірування з крейсерської
    висоти під 60° гарантовано мажe по рухомій машині — вона встигає вийти з-під дрона.
    І ще: біля цілі захист від землі треба вимикати, інакше промах завжди вертикальний.

---

## 12. Критерій «готово» для польоту

Перед M2 сісти і перевірити руками:
1. Зависання на місці потребує постійної корекції — але не бісить.
2. Різкий roll-стік → дрон продовжує зносити вбік ще ~1 с (інерція є).
3. Важкий дрон *очевидно* важчий за легкий наосліп, без підписів.
4. Пікірування з газом 20% дає видиму тряску пропвошу.
5. Політ повз дерево на 5 м/с виглядає страшно.

Якщо всі 5 — так, рухаємось далі.

---

## 13. Тон

Гра про **навичку та увагу**, не про фраги. Немає рахунку, комбо, крові, ефектних вибухів,
героїчної музики. Влучання — коротка втрата сигналу і чорний екран, потім ~6 с сухого
ЧБ-повтору з борта спостерігача (підліт, виблиск, дим — будь-яка клавіша пропускає).
Дебриф — суха таблиця: час, дистанція, витрата, результат. Це симулятор ремесла,
і саме тому він має бути важким.
